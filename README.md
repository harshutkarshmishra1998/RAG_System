## System Dependencies

This project requires the following system-level tools:

### Windows
- Poppler (PDF rendering)
- Tesseract OCR

These must be installed and added to PATH.

### Linux / Docker
Installed via:
- poppler-utils
- tesseract-ocr
- ghostscript